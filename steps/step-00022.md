# Step 00022 — Install @pooling-poc/unpooled-app dependencies

## Commands

Run from the repo root:

```bash
pnpm --filter @pooling-poc/unpooled-app add @pooling-poc/db-query@workspace:* @nestjs/terminus @nestjs/axios axios nestjs-pino pino pino-http
pnpm --filter @pooling-poc/unpooled-app add -D @types/node
```

## Expected result
`apps/unpooled-app/package.json` has `@pooling-poc/db-query` listed as a dependency with `"workspace:*"`.
