# Step ✅ 00019 — Update AppModule in pooled-app

## Context

Replace the generated `app.module.ts` to import `DbQueryModule`, `LoggerModule` (nestjs-pino), `HealthModule`, and register `ProcesslistController`.

## Action

Replace the entire contents of `apps/pooled-app/src/app.module.ts` with:

```typescript
import { Module } from '@nestjs/common'
import { LoggerModule } from 'nestjs-pino'
import { DbQueryModule } from '@pooling-poc/db-query'
import { ProcesslistController } from './processlist.controller'
import { HealthModule } from './health.module'

@Module({
  imports: [LoggerModule.forRoot(), DbQueryModule, HealthModule],
  controllers: [ProcesslistController],
})
export class AppModule {}
```

## Expected result

`apps/pooled-app/src/app.module.ts` imports `DbQueryModule`, `LoggerModule`, and `HealthModule`, and registers `ProcesslistController`.
