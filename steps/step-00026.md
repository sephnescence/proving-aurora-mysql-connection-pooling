# Step 00026 — Update AppModule in unpooled-app

## Action

Replace the entire contents of `apps/unpooled-app/src/app.module.ts` with:

```typescript
import { Module } from '@nestjs/common'
import { LoggerModule } from 'nestjs-pino'
import { DbQueryModule } from '@pooling-poc/db-query'
import { ProcesslistController } from './processlist.controller'
import { HealthModule } from './health.module'

@Module({
  imports: [LoggerModule.forRoot(), DbQueryModule, HealthModule],
  controllers: [ProcesslistController],
})
export class AppModule {}
```

## Expected result

`apps/unpooled-app/src/app.module.ts` imports `DbQueryModule`, `LoggerModule`, and `HealthModule`, and registers `ProcesslistController`.
