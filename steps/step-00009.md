# ✅ Step 00009 — Create ProcesslistRow type

## Context
Define the shared data type representing one row from `information_schema.processlist`.

## Action
Create the file `packages/db-query/src/processlist-row.interface.ts` with the following exact content:

```typescript
export interface ProcesslistRow {
  ID: number;
  USER: string;
  HOST: string;
  DB: string | null;
  COMMAND: string;
  TIME: number;
  STATE: string | null;
  INFO: string | null;
}
```

## Expected result
`packages/db-query/src/processlist-row.interface.ts` exists with the interface exported.
