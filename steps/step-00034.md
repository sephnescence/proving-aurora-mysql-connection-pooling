# Step 00034 — Install @pooling-poc/load-tests dependencies

## Commands

Run from the repo root:

```bash
pnpm --filter @pooling-poc/load-tests add -D artillery typescript tsx @types/node @aws-sdk/client-cloudwatch-logs
```

## Expected result
`packages/load-tests/package.json` has `devDependencies` with `artillery`, `typescript`, `tsx`, and `@aws-sdk/client-cloudwatch-logs`.
