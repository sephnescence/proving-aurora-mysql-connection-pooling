# Step 00008 — Create packages/db-query/vitest.config.ts

## Context
Vitest configuration for the db-query unit tests.

## Action
Create the file `packages/db-query/vitest.config.ts` with the following exact content:

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
  },
});
```

## Expected result
`packages/db-query/vitest.config.ts` exists. Running `pnpm --filter @pooling-poc/db-query test` will use this config.
