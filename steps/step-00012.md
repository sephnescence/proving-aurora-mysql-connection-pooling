# Step 00012 — Create packages/db-query barrel export

## Context
The public API of the package, re-exporting everything consumers need.

## Action
Create the file `packages/db-query/src/index.ts` with the following exact content:

```typescript
export { DbQueryModule } from './db-query.module';
export { DbQueryService } from './db-query.service';
export { ProcesslistRow } from './processlist-row.interface';
```

## Expected result
`packages/db-query/src/index.ts` exists. Other packages can import from `@pooling-poc/db-query`.
