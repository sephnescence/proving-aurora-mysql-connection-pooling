# Step 00035 — Create packages/load-tests/tsconfig.json

## Action
Create the file `packages/load-tests/tsconfig.json` with the following exact content:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": ".",
    "noEmit": true,
    "module": "ESNext",
    "moduleResolution": "bundler"
  },
  "include": ["*.ts"],
  "exclude": ["node_modules"]
}
```

## Expected result
`packages/load-tests/tsconfig.json` exists.
