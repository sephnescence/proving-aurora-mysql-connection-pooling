# Step 00029 — Create packages/integration-tests/tsconfig.json

## Action

Create the file `packages/integration-tests/tsconfig.json` with the following exact content:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "noEmit": true
  },
  "include": ["src"],
  "exclude": ["node_modules"]
}
```

## Expected result

`packages/integration-tests/tsconfig.json` exists.
