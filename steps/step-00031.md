# Step 00031 — Create packages/integration-tests/vitest.config.ts

## Action

Create the file `packages/integration-tests/vitest.config.ts` with the following exact content:

```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    testTimeout: 30000,
  },
})
```

## Expected result

`packages/integration-tests/vitest.config.ts` exists.
